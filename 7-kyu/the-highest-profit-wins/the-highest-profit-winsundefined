#include <utility>
#include <vector>
#include <climits>
​
std::pair<int, int> min_max(const std::vector<int>& arr)
{
  int min=INT_MAX;
  int max=INT_MIN;
  for(int i=0;i<arr.size();i++){
    if(arr[i]>max)
      max=arr[i];
    if(arr[i]<min)
      min=arr[i];
  }
    return { min, max}; 
}